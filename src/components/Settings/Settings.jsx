import styles from './Settings.module.scss'

const Settings = (props) => {
    return (
        <div>
            <h1 className={styles.active}>Settings page</h1>
        </div>
    )
}
export default Settings;
